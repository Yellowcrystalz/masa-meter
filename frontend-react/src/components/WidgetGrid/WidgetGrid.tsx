import MeterWidget from "@components/widgets/MeterWidget/MeterWidget.tsx"

const WidgetGrid = () => {
    return (
        <div>
            <MeterWidget />
        </div>
    );
}

export default WidgetGrid;
